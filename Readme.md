# ETokenSign

ETokenSign is a replacement for Microsoft's SignTool.exe, written in Rust, specifically for signing files with an EV Token, without requiring user intervention.  This is intended to make it easier to integrate EV signing certificates with Continuous Integration build servers.